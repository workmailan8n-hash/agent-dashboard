import { describe, it, expect } from 'vitest';

describe('pathfinding — smoke test', () => {
  it('true is true', () => {
    expect(true).toBe(true);
  });

  it('placeholder: replace with real A* tests (wall collision, 6s timeout, separation force)', () => {
    const walkTimeoutMs = 6000;
    expect(walkTimeoutMs).toBe(6000);
  });
});
